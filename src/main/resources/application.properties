# Conexión a MySQL Local
spring.datasource.url=jdbc:mysql://localhost:3306/myfinance?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=rusky

# Configuración de Hibernate para MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Evita que Jackson falle al encontrarse con proxies (Mantenido de tu config original)
spring.jackson.serialization.fail-on-empty-beans=false

# Evita que Jackson falle al encontrarse con proxies de carga perezosa (lazy loading)
spring.jackson.serialization.fail-on-empty-beans=false